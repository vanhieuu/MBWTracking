import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const TravelHistory = (props: Props) => {
  return (
    <View>
      <Text>TravelHistory</Text>
    </View>
  )
}

export default TravelHistory

const styles = StyleSheet.create({})